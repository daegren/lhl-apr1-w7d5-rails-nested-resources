<h1>All Comments</h1>

<% @comments.each do |comment| %>

<div>
  <p><%= comment.body %></p>
  <%= link_to 'Delete Comment', [:admin, comment], method: :delete, data: { confirm: 'Are you sure?' }  %>
</div>

<% end %>
